{
  "csharpCode": "var plot = UnityEngine.Object.FindAnyObjectByType<FarmPrototype.Farming.FarmTestPlot>(); var hud = UnityEngine.Object.FindAnyObjectByType<FarmPrototype.Farming.FarmHudController>(); if (plot == null || hud == null || plot.GameState == null) throw new System.Exception(\"Base da fazenda ausente\"); var state = plot.GameState; var original = state.CreateSaveData(null); try { var migrated = new FarmPrototype.Farming.FarmSaveData { Version = 3, Money = 125, Inventory = original.Inventory, Tiles = original.Tiles, Hotbar = original.Hotbar, SelectedHotbarIndex = original.SelectedHotbarIndex }; state.Restore(migrated); if (state.Tutorial == null || state.Tutorial.CompletedCount != 0 || state.Tutorial.RewardClaimed) throw new System.Exception(\"Migracao v3 nao iniciou o guia limpo\"); var initialMoney = state.Money; if (!state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.Tilled)) throw new System.Exception(\"Marco de preparo nao avancou\"); if (state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.Tilled)) throw new System.Exception(\"Marco duplicado foi contado\"); state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.Planted); state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.Watered); state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.Harvested); state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.Sold); if (state.Money != initialMoney || state.Tutorial.CompletedCount != 5) throw new System.Exception(\"Recompensa saiu antes da conclusao\"); state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.BoughtSeeds); if (!state.Tutorial.IsComplete || !state.Tutorial.RewardClaimed || state.Money != initialMoney + 50) throw new System.Exception(\"Conclusao ou bonus de 50 falhou\"); state.MarkMilestone(FarmPrototype.Farming.FarmMilestone.BoughtSeeds); if (state.Money != initialMoney + 50) throw new System.Exception(\"Bonus foi entregue duas vezes\"); var saved = state.CreateSaveData(null); if (saved.Version != 4 || saved.Tutorial == null || !saved.Tutorial.IsComplete || !saved.Tutorial.RewardClaimed) throw new System.Exception(\"Save v4 nao preservou o guia\"); state.Restore(saved); if (!state.Tutorial.IsComplete || state.Money != initialMoney + 50) throw new System.Exception(\"Restore v4 perdeu progresso\"); var panel = hud.transform.Find(\"Farm_UI/FirstHarvest\"); if (panel == null) throw new System.Exception(\"Painel Primeira Colheita ausente\"); UnityEngine.Debug.Log(\"[FARM_TUTORIAL_V4_OK] 6 marcos | ordem flexivel | bonus unico $50 | migracao v3 | save/restore | HUD\"); } finally { state.Restore(original); plot.SelectHotbarSlot(original.SelectedHotbarIndex); }",
  "className": "FarmTutorialV4Validation",
  "methodName": "Run",
  "parameters": [],
  "isMethodBody": true
}
